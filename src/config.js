// Change depending on the logic of your websocket.
// In this case, the websocket identifies /control and /overlay as "source" and "target"
const config = {
  websocketUrl: "ws://localhost:8080/control",
};

export default config;
